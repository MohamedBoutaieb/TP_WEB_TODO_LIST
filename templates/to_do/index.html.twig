<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootswatch@4.5.2/dist/cyborg/bootstrap.min.css">
    <title>TO DO LIST TP </title>
</head>

<body>
<div class="container">
    {% for value in app.session.flashBag.get('info') %}
        <div class="alert alert-info">
            {{ value }}
        </div>
    {% endfor %}
    {% for value in app.session.flashBag.get('success') %}
        <div class="alert alert-success">
            {{ value }}
        </div>
    {% endfor %}
    {% for value in app.session.flashBag.get('error') %}
        <div class="alert alert-danger">
            {{ value }}
        </div>
    {% endfor %}
    <h1 style="text-align: center;"> To Do List</h1>
    <ol class="list-group">
        {% for name,description in app.session.get('todos') %}
        <li class="list-group-item" style="background-color: rgb(73, 44, 211); ">
            {{ name }} : {{ description }}
        </li>
    </ol>
    {% endfor %}
    <h2>hints:</h2>  <br>
    -todo/updateName/name/newName to update the task's name ( you can add /content to modify content as well)<br>
    -todo/update/name/content to update the task's content<br>
    -todo/delete/name to delete a task<br>
    -todo/add/name/content to add a task<br>
</div>


</body>
</html>
